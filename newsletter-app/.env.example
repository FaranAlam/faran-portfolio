# ==================== Newsletter & Contact Management Backend ====================

# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/newsletter

# CORS Configuration (comma-separated for multiple origins)
CORS_ORIGIN=http://127.0.0.1:5500,http://localhost:5500,https://your-netlify-domain.netlify.app

# JWT Authentication Configuration
# IMPORTANT: Change JWT_SECRET to a strong random string in production
# Generate one: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-string-in-production
JWT_EXPIRE=7d

# Email Configuration (SMTP)
# For Gmail: Use App Password (not regular password)
# 1. Enable 2-Factor Authentication
# 2. Create App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password-here
SMTP_FROM=your-email@gmail.com

# Contact Form Notification Email
CONTACT_TO=faranalam14203@gmail.com

# ==================== Security Notes ====================
# - Never commit the actual .env file to Git (it's in .gitignore)
# - Use strong JWT_SECRET in production (minimum 64 characters)
# - Use Gmail App Password, not your regular Gmail password
# - Keep MongoDB connection string secure
# - Update CORS_ORIGIN with your actual domain after deployment
