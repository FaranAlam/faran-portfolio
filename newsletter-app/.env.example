# Server
PORT=5000
# Comma-separated list of allowed origins for CORS
# e.g. http://localhost:5500,https://yourdomain.com
CORS_ORIGIN=http://localhost:5500

# MongoDB
MONGO_URI=mongodb://localhost:27017/newsletter

# SMTP (Email)
# For Gmail, use smtp.gmail.com with an App Password
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your_username_or_email@example.com
SMTP_PASS=your_smtp_password_or_app_password
# Optional. Defaults to SMTP_USER if not set
SMTP_FROM=Your Name <your_username_or_email@example.com>
# Contact form destination (defaults to SMTP_USER if empty)
CONTACT_TO=recipient@example.comPORT=5000
MONGO_URI=mongodb://localhost:27017/newsletter
CORS_ORIGIN=http://localhost:3000

CONTACT_TO=you@example.com

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=your_app_password
SMTP_FROM="Your Name <you@example.com>"

ADMIN_USER=admin
ADMIN_PASS=supersecret
ADMIN_TOKEN=changeme
